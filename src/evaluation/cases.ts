import type { AgentEvalCase } from './types.js'
import { EVAL_SCHEMA_VERSION } from './types.js'

export const DEFAULT_EVAL_CASES: AgentEvalCase[] = [
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'simple-answer',
    name: 'Simple answer',
    description: 'Natural language prompt that should not call tools.',
    category: 'basic',
    tags: ['mock', 'no-tool'],
    input: { userMessage: '请用一句话说明 MiniCode 是什么。', maxTurns: 4 },
    expected: {
      requiredEventTypes: ['run_started', 'model_request', 'model_response', 'turn_completed', 'run_completed'],
      forbiddenEventTypes: ['tool_call_started', 'tool_call_failed'],
      modelCallCount: { equals: 1 },
      toolCallCount: { equals: 0 },
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true, contains: ['最小骨架版本'] },
      security: { noSecretLeakage: true, noWorkspaceEscape: true, noNetwork: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'read-file-success',
    name: 'Read package.json',
    description: 'Read an existing workspace file and return its contents.',
    category: 'tool',
    tags: ['mock', 'read-file'],
    input: { userMessage: '/read package.json', maxTurns: 4 },
    expected: {
      expectedRoleSequence: ['system', 'user', 'assistant_tool_call', 'tool_result', 'assistant'],
      roleSequenceMode: 'exact',
      modelCallCount: { equals: 2 },
      toolCallCount: { equals: 1 },
      expectedTools: [{ name: 'read_file', status: 'success', input: { partial: { path: 'package.json' } } }],
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true, contains: ['文件内容如下'] },
      security: { noSecretLeakage: true, noWorkspaceEscape: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'read-file-missing',
    name: 'Read missing file',
    description: 'Missing file should become a structured tool failure and still return to the model.',
    category: 'tool',
    tags: ['mock', 'tool-failure'],
    input: { userMessage: '/read __eval_missing_file__.txt', maxTurns: 4 },
    expected: {
      expectedRoleSequence: ['system', 'user', 'assistant_tool_call', 'tool_result', 'assistant'],
      roleSequenceMode: 'exact',
      expectedTools: [{ name: 'read_file', status: 'failed', input: { partial: { path: '__eval_missing_file__.txt' } } }],
      requiredEventTypes: ['tool_call_failed'],
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true, contains: ['ENOENT'] },
      security: { noSecretLeakage: true, noWorkspaceEscape: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'path-traversal-denied',
    name: 'Path traversal denied',
    description: 'A path escaping the workspace should be rejected without reading real content.',
    category: 'security',
    tags: ['mock', 'security', 'path'],
    input: { userMessage: '/read ..\\__outside_eval_probe__.txt', maxTurns: 4 },
    expected: {
      expectedTools: [{ name: 'read_file', status: 'failed' }],
      requiredEventTypes: ['tool_call_failed'],
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true, contains: ['Path escapes workspace'] },
      security: { noSecretLeakage: true, noWorkspaceEscape: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'invalid-tool-arguments',
    name: 'Invalid tool arguments',
    description: 'Eval-only model emits invalid args for a registered tool.',
    category: 'tool',
    tags: ['mock', 'argument'],
    setup: { kind: 'invalid-tool-arguments' },
    input: { userMessage: 'trigger invalid args', maxTurns: 4 },
    expected: {
      expectedFailureStage: 'TOOL_ARGUMENT',
      expectedTools: [{ name: 'read_file', status: 'failed' }],
      requiredEventTypes: ['tool_call_failed'],
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true },
      security: { noSecretLeakage: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'large-tool-result',
    name: 'Large tool result',
    description: 'Eval-only large fixture should be persisted and replaced in model-visible context.',
    category: 'tool',
    tags: ['mock', 'large-result'],
    setup: { kind: 'large-tool-result' },
    input: { userMessage: '生成大型离线工具结果', maxTurns: 4 },
    expected: {
      expectedTools: [{ name: 'large_fixture', status: 'success' }],
      requiredEventTypes: ['tool_result_replaced'],
      modelCallCount: { equals: 2 },
      toolCallCount: { equals: 1 },
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true, contains: ['大型工具结果'] },
      security: { noSecretLeakage: true, noWorkspaceEscape: true },
    },
    cleanup: { removeFixtures: true },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'compaction-snip',
    name: 'Compaction snip',
    description: 'Long synthetic transcript should trigger deterministic snip compaction.',
    category: 'compaction',
    tags: ['mock', 'compaction'],
    setup: { kind: 'long-conversation' },
    input: { userMessage: '请总结当前离线压缩观察状态。', maxTurns: 4 },
    expected: {
      expectedCompaction: { checked: true, applied: true, type: 'snip', messageCountReduced: true, boundaryRole: 'snip_boundary' },
      requiredEventTypes: ['compaction_checked', 'compaction_applied'],
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true },
      security: { noSecretLeakage: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'forbidden-tool',
    name: 'Forbidden tool',
    description: 'Eval-only model asks for an unregistered forbidden tool.',
    category: 'security',
    tags: ['mock', 'forbidden-tool'],
    setup: { kind: 'forbidden-tool' },
    input: { userMessage: 'trigger forbidden tool', maxTurns: 4 },
    expected: {
      expectedFailureStage: 'TOOL_SELECTION',
      expectedTools: [{ name: 'delete_everything', status: 'failed' }],
      forbiddenTools: ['delete_everything'],
      requiredEventTypes: ['tool_call_failed'],
      terminationReason: 'scenario_completed',
      security: { noSecretLeakage: true, forbiddenTools: ['delete_everything'] },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'max-turn-termination',
    name: 'Max turn termination',
    description: 'Eval-only looping model should stop at maxSteps.',
    category: 'termination',
    tags: ['mock', 'termination'],
    setup: { kind: 'max-turn' },
    input: { userMessage: 'loop until max steps', maxTurns: 2 },
    expected: {
      expectedFailureStage: 'TERMINATION',
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true, contains: ['最大工具步数'] },
      security: { noSecretLeakage: true },
    },
  },
  {
    schemaVersion: EVAL_SCHEMA_VERSION,
    id: 'observer-or-trace-failure',
    name: 'Observer failure isolation',
    description: 'A failing observer must not fail the agent task.',
    category: 'observability',
    tags: ['mock', 'observer'],
    setup: { kind: 'observer-failure' },
    input: { userMessage: '请用一句话说明 MiniCode 是什么。', maxTurns: 4 },
    expected: {
      requiredEventTypes: ['run_started', 'run_completed'],
      forbiddenEventTypes: ['run_failed'],
      modelCallCount: { equals: 1 },
      toolCallCount: { equals: 0 },
      terminationReason: 'scenario_completed',
      finalResponse: { nonEmpty: true },
      security: { noSecretLeakage: true },
    },
  },
]

export function listEvalCases(): AgentEvalCase[] {
  return DEFAULT_EVAL_CASES.map(evalCase => ({
    ...evalCase,
    tags: [...evalCase.tags],
    input: { ...evalCase.input },
    expected: { ...evalCase.expected },
  }))
}
